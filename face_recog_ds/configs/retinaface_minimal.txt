# nvinfer config minimal pour POC RetinaFace.
# Pas de parsing custom des sorties pour l'instant — on veut juste valider
# que le pipeline tourne et que la GPU bosse.

[property]
gpu-id=0
model-engine-file=/work/engines/det_10g.fp16.engine
infer-dims=3;640;640
net-scale-factor=0.0078125
offsets=127.5;127.5;127.5
model-color-format=1
network-mode=2
network-type=100
output-tensor-meta=1
cluster-mode=4
batch-size=1
gie-unique-id=1
